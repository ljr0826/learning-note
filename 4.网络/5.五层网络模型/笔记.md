# 五层网络模型

应用层：软件上会用到的协议，HTTP 协议、DNS 协议（访问域名对应的 IP）

运输层：TCP 协议、UDP 协议

网络层：IP 地址---IP 协议

数据链路层：硬件层面，做传输的

物理层：定义数据传输的介质材料（光纤/铜/。。。），硬件层面，做传输的

## HTTP 协议

http 协议约定了请求和响应的消息格式：

请求 request

请求 路径+参数(query+hash) 协议版本

响应 response

协议版本 状态码 状态文本
