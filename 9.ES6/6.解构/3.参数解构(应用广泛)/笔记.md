# 参数解构

解构的语法规则，其实已经讲完了。

但是由于解构的语法最常见的就是函数参数解构，所以重点讲一下。尤其是大量的配置对象的传参最常见

```js
// function print(user) {
//   console.log(`姓名：${uesr.name}`);
//   console.log(`年龄：${uesr.age}`);
//   console.log(`性别：${uesr.sex}`);
//   console.log(`省份：${uesr.address.province}`);
//   console.log(`城市：${uesr.address.city}`);
// } //以前的写法：太low了

function print({ name, age, sex, address: { province, city } }) {
  //直接解构：因为知道传递的参数一定是一个对象
  console.log(`姓名：${name}`);
  console.log(`年龄：${age}`);
  console.log(`性别：${sex}`);
  console.log(`省份：${province}`);
  console.log(`城市：${city}`);
}
const user = {
  name: "kevin",
  age: 11,
  sex: "男",
  address: {
    province: "四川",
    city: "成都",
  },
};
print(user);
```

```js
//之前的写法
 function ajax(options) {
   const defaultOptions = {
     method: "get",
     url: "/",
   }; //默认配置
   const opt = {
     ...defaultOptions,
     ...options,
   }; //最终的配置
   console.log(opt);
 }
 //解构的方法
 function ajax({ method = "get", url = "/" } = {}) {
   //特别注意：若不传参，默认从{}中解构(使用参数默认值，解决不传参的问题)，因为从undefined解构东西会报错
   console.log(method, url);
 }
 ajax({
   method: "get",
   url: "/abc",
 });
```